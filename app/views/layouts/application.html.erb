<!DOCTYPE html>
<html>
  <head>
    <title>Rails7 + Vite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body>
    <div class="flex flex-col h-screen">
      <%= render "shared/navbar" %>
      <%= render "shared/flash" %>
      <main class="container flex flex-col flex-1 px-5 mx-auto my-12">
        <%= yield %>
      </main>
      <%= render "shared/footer" %>
    </div>
  </body>
</html>
